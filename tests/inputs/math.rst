####
Math
####

.. theorem::

   This is the statement of a theorem and it contains math.  All `X` are `Y` except when
   they are `Z`.

.. proof::

   :step: And this is a proof step that also contains math `\forall X \colon Y \iff Z`.


.. theorem::

   `X` if and only if `Y`.

.. proof::

   :step: `\implies`

      :step: First sub-step.  Note the first step contains a single latex symbol.

         :p: And this is the proof of the first sub-step.

   :step: `\impliedby`

      :step: Sub-step here.

      :step: And another sub-step here.
